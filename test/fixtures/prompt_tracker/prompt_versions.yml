greeting_v1:
  prompt: greeting
  template: "Hello {{name}}, how can I help you today?"
  version_number: 1
  status: active
  source: file
  variables_schema:
    - name: name
      type: string
      required: true
  model_config: {}
  notes: "Original version"

greeting_v2:
  prompt: greeting
  template: "Hi {{name}}! Need help?"
  version_number: 2
  status: draft
  source: web_ui
  variables_schema:
    - name: name
      type: string
      required: true
  model_config: {}
  notes: "Shorter version for testing"

summary_v1:
  prompt: summary
  template: "Summarize this conversation: {{conversation}}"
  version_number: 1
  status: active
  source: file
  variables_schema:
    - name: conversation
      type: string
      required: true
  model_config: {}
  notes: "Initial version"

